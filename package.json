{
  "name": "peacemakr-website",
  "version": "1.0.0",
  "description": "peacemakr.io website",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf ./.cache ./localdev",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run clean && parcel -d ./localdev src/index.html --public-url /",
    "build": "npm run clean && parcel build -d ./prod src/index.html --public-url /"
  },
  "author": "",
  "license": "ISC",
  "homepage": "https://peacemakr.io",
  "dependencies": {
    "@tailwindcss/postcss7-compat": "^2.0.2",
    "@tailwindcss/typography": "^0.4.0",
    "autoprefixer": "^9.8.6",
    "postcss": "^7.0.35",
    "tailwindcss": "npm:@tailwindcss/postcss7-compat@^2.0.2"
  },
  "devDependencies": {
    "parcel-bundler": "latest",
    "postcss-modules": "latest"
  }
}
